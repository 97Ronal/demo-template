import React from 'react';
import ReactDOM from 'react-dom/client';
import { TodoList as App } from './components/index.tsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <App />
);

